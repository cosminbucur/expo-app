// TODO: CRUD api

export const getBooks = (book) => console.log('GET /books', book);

export const addBook = (book) => console.log('POST /books', book);

export const editBook = (book) => console.log('PUT /books', book);

export const deleteBook = (id) => console.log('DELETE /books', id);
